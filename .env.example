# ═══════════════════════════════════════════════════════════════════════════
# TablHero .env.example - Template per variabili d'ambiente
# 
# ISTRUZIONI:
# 1. Copia questo file: cp .env.example .env
# 2. Modifica i valori con le tue credenziali
# 3. NON committare .env (contiene secrets!)
# 
# ═══════════════════════════════════════════════════════════════════════════

# ───────────────────────────────────────────────────────────────────────────
# FLASK CONFIGURATION
# ───────────────────────────────────────────────────────────────────────────

# Ambiente: 'development' (locale) o 'production' (cloud)
FLASK_ENV=development

# Debug mode: True (sviluppo) o False (produzione)
FLASK_DEBUG=True

# Chiave segreta Flask (genera con: python -c "import secrets; print(secrets.token_hex(16))")
# ⚠️  IMPORTANTE: Cambia questo in produzione!
SECRET_KEY=dev-key-change-in-production-tablhero-2025


# ───────────────────────────────────────────────────────────────────────────
# DATABASE
# ───────────────────────────────────────────────────────────────────────────

# SVILUPPO: SQLite (file locale, niente da fare)
# DATABASE_URL=sqlite:///tablhero.db

# PRODUZIONE: PostgreSQL (Railway, Heroku, ecc)
# DATABASE_URL=postgresql://user:password@host:5432/tablhero

# PRODUZIONE: MySQL (AWS RDS, Azure, ecc)
# DATABASE_URL=mysql+pymysql://user:password@host:3306/tablhero

# Lascia commentato per sviluppo locale (usa SQLite di default)
# DATABASE_URL=


# ───────────────────────────────────────────────────────────────────────────
# STRIPE - Payment Processing
# ───────────────────────────────────────────────────────────────────────────

# Ottenere da: https://dashboard.stripe.com/test/apikeys
STRIPE_PUBLIC_KEY=pk_test_your_public_key_here
STRIPE_SECRET_KEY=sk_test_your_secret_key_here
STRIPE_WEBHOOK_SECRET=whsec_test_your_webhook_secret_here


# ───────────────────────────────────────────────────────────────────────────
# EMAIL CONFIGURATION
# ───────────────────────────────────────────────────────────────────────────

# SMTP Server (Gmail, SendGrid, ecc)
MAIL_SERVER=smtp.gmail.com

# SMTP Port
MAIL_PORT=587

# Use TLS (True/False)
MAIL_USE_TLS=True

# SMTP Username (email)
MAIL_USERNAME=your-email@gmail.com

# SMTP Password (per Gmail: usa app-specific password)
# Vedi: https://support.google.com/accounts/answer/185833
MAIL_PASSWORD=your-app-specific-password

# Default sender email
MAIL_DEFAULT_SENDER=noreply@tablhero.com


# ───────────────────────────────────────────────────────────────────────────
# LOGGING
# ───────────────────────────────────────────────────────────────────────────

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO


# ═══════════════════════════════════════════════════════════════════════════
# ISTRUZIONI PER I VALORI
# ═══════════════════════════════════════════════════════════════════════════

# 1. SECRET_KEY:
#    Genera una nuova chiave: python -c "import secrets; print(secrets.token_hex(32))"
#    Cambia prima di deployare in produzione!

# 2. DATABASE_URL:
#    - Sviluppo: Lascia commentato (usa SQLite locale)
#    - Produzione: Fornita da Railway/Heroku/provider

# 3. STRIPE_*:
#    - Test: Usa chiavi di test (pk_test_, sk_test_)
#    - Produzione: Usa chiavi live (pk_live_, sk_live_)
#    - Ottenere da: https://dashboard.stripe.com

# 4. MAIL_USERNAME/PASSWORD:
#    - Per Gmail: Usa app-specific password (non la password normale!)
#    - Per altre piattaforme: Usa credenziali SMTP

# 5. FLASK_ENV:
#    - development: Modalità sviluppo (auto-reload, debug abilitato)
#    - production: Modalità produzione (debug disabilitato)

# ═══════════════════════════════════════════════════════════════════════════
